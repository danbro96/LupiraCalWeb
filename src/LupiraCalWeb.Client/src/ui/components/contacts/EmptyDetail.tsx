/** Placeholder shown in the detail pane when no contact or group is selected. */
export function EmptyDetail() {
  return (
    <div className="contacts-detail-pane">
      <p className="empty">Select a contact or group.</p>
    </div>
  );
}
