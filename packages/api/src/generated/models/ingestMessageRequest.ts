/**
 * Generated by orval 🍺
 * Do not edit manually.
 * LupiraCalWeb BFF
 * OpenAPI spec version: v1
 */
import type { MessageSource } from './messageSource.ts';

/**
 * A normalised message from a connector sidecar (or the historical importer). The connector
 * guarantees string IngestMessageRequest.SourceRef is unique within MessageSource IngestMessageRequest.Source for the principal — it
 * is the dedup key. string IngestMessageRequest.ConversationRef groups messages into a thread; the sender fields
 * resolve a `Participant`.
 */
export interface IngestMessageRequest {
  /** The owner this message belongs to — relayed by the service caller, never inferred. */
  principalId: string;
  source: MessageSource;
  /** The platform message id (dedup identity within MessageSource IngestMessageRequest.Source). */
  sourceRef: string;
  text: string;
  /** When the message was sent on the platform. */
  timestamp: string;
  /** The platform thread/chat id this message belongs to. */
  conversationRef: string;
  /**
     * Optional thread title (set when the conversation is first seen).
     * @nullable
     */
  conversationTitle?: string | null;
  /**
     * The replied-to message's string IngestMessageRequest.SourceRef, when the platform carries one —
     *             the topic engine attaches a reply to its parent's topic unconditionally.
     * @nullable
     */
  replyToSourceRef?: string | null;
  /**
     * Optional platform sender id; resolves/creates a `Participant` when present.
     * @nullable
     */
  senderRef?: string | null;
  /** @nullable */
  senderName?: string | null;
  /**
     * True when the owner sent this message (the connector's direction bit). Defaults false, so
     *             older/importer callers that don't set it are treated as received.
     */
  fromPrincipal?: boolean;
  /**
     * Set by the historical importer to tag the import run; null for live ingest.
     * @nullable
     */
  importBatch?: string | null;
  /**
     * Export-root-relative path of the message's media file, if any — the media import
     *             phase uploads it and stamps the row's object key. Null for live ingest (sidecars skip media).
     * @nullable
     */
  attachmentPath?: string | null;
}
